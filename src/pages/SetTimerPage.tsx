import SelectMinutes from '../components/SelectMinutes';

export default function SetTimerPage() {
  return (
    <section className='w-full min-h-screen bg-grey text-white flex flex-col items-center justify-center'>
      <SelectMinutes />
    </section>
  );
}
